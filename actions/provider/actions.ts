import { Provider } from '../../types/provider';

export const createProvider = async (data: Provider) => {
    // Implementation for creating a new provider
};

export const readProvider = async (id: string) => {
    // Implementation for reading a provider by id
};

export const updateProvider = async (id: string, data: Partial<Provider>) => {
    // Implementation for updating a provider by id
};

export const deleteProvider = async (id: string) => {
    // Implementation for deleting a provider by id
}; 