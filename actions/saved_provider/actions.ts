import { SavedProvider } from '../../types/saved_provider';

export const createSavedProvider = async (data: SavedProvider) => {
    // Implementation for creating a new saved provider
};

export const readSavedProvider = async (id: string) => {
    // Implementation for reading a saved provider by id
};

export const updateSavedProvider = async (id: string, data: Partial<SavedProvider>) => {
    // Implementation for updating a saved provider by id
};

export const deleteSavedProvider = async (id: string) => {
    // Implementation for deleting a saved provider by id
}; 